# AIVibe — План апгрейда (пересборки) проекта

> Рабочий документ для владельца и команды.
> Цель апгрейда: **закрыть разрыв «документы ↔ код» и поднять монетизацию**, сохранив рабочее ядро.
> Это **не переписывание с нуля.** Логика ядра (AgentLoop, ToolRegistry, Triplex Fallback, DesignNorms, AR-движок) — актив, его не трогаем, а достраиваем вокруг.
> Фундамент плана: `docs/BUSINESS_MODEL.md` (деньги) + `docs/UX_GROWTH.md` (рост) + **аудит реального кода** (8 подсистем, ссылки `файл:строка`).
> Все оценки трудозатрат — ориентир, не обещание.

## Changelog

| Дата | Версия | Изменения |
|------|--------|-----------|
| 2026-06-09 | 1.0 | Первая версия: бэкап, аудит код↔доки, план по 5 фазам, защищённое ядро, риски, открытые вопросы |
| 2026-06-09 | 1.1 | Зафиксированы решения владельца: стриминг отложен, гибрид AI → Фаза 2, CPA → решить на старте Фазы 3, оцифровка бесплатна для первых партнёров |

**Легенда трудозатрат:** S < 1 дня · M ~ несколько дней · L ~ 1–2 недели · XL > 2 недель.
**🔒 = затрагивает защищённое ядро** (по `CLAUDE.md` — только bugfix/добавления, нужно согласование владельца).

---

## 0. Бэкап и откат (сделано до начала работ)

Текущая версия сохранена в git **перед** любыми изменениями:

| Что | Имя | Коммит |
|-----|-----|--------|
| Тег-снимок | `v1-pre-upgrade` | `de7d957` |
| Ветка-архив | `archive/v1-pre-upgrade` | `de7d957` |

**Откат в любой момент:** `git checkout v1-pre-upgrade` (или `git checkout archive/v1-pre-upgrade`).
Бэкап **локальный** (не запушен). Дубль на GitHub при желании: `git push origin v1-pre-upgrade archive/v1-pre-upgrade` (отдельное подтверждение).

---

## 1. Главные выводы аудита (что важнее, чем в доках)

Аудит сверил каждое утверждение доков с реальным кодом. Три находки меняют расстановку приоритетов:

1. **Зрелый движок расстановки подключён НЕ туда.** Детерминированный движок (`GenerateArrangementTool` + `CollisionService` + `DesignNorms`) реален и качественен, но работает **только в чат-советнике** (через `ToolRegistry`/`SkillIndex`). На экране **AR-расстановки его нет** — там координаты мебели **придумывает сам LLM** (`AgentOrchestrator` → `PromptBuilder.swift:85` явно просит у модели `position {x,y,z}`), а коллизии лишь валидируются с **повторным запросом к LLM** (до 2 раз). Отсюда медленная и нестабильная генерация. → **Главный технический актив есть, его нужно переключить на нужный путь**, а не строить заново.

2. **«Путь без LiDAR» хуже, чем заглушка.** Кнопка «Ввести вручную» ведёт в пустоту (`.manualEntryTapped` → `.none`), формы ввода размеров нет. Хуже: **рантайм-проверки наличия LiDAR нет вообще** — единственная развилка `#if targetEnvironment(simulator)`. На реальном iPhone **без** LiDAR безусловно запускается RoomPlan и **молча не даёт результата** — пользователь в тупике без объяснения. Это отсекает массовый рынок РФ (**разрыв №1**).

3. **Монетизации в коде нет вообще.** Сквозной поиск по всему репозиторию: `ЮKassa / промокод / paywall / CPA / подписка` — **ноль** в коде (только в документах и в отдельном Next.js-демо в долларах). Единственные «денежные» следы — **мёртвый** флаг `hasSubscription` (объявлен, нигде не читается) и заблокированный в MVP `confirm_purchase_order` (always-DENY). Весь контур дохода строится с нуля.

Дополнительно подтверждено: **нет каталога USDZ** (0 моделей в бандле → рендерится placeholder-бокс); **бэкенд скрейпит WB/Ozon** (Apify) вместо партнёров; **«нативный вектор YDB»** на деле — `scan(limit 500)` + косинус в Node (нарушает собственное правило §9.2); **гибрид AI Lite+Pro не реализован** (всё уходит на дорогую Pro-модель); **фаза 10 сделана наполовину** (чат сохраняется, проекты комнат — нет, и хранилище в `Caches`, который ОС может вычистить).

---

## 2. Карта «документ ↔ код»

Статус: ✅ построено · ⚠️ частично · ❌ нет в коде. Evidence — ключевые файлы (полные ссылки `файл:строка` — в фазах ниже).

| Подсистема | Утверждение доков | Реальность | Файлы |
|---|---|---|---|
| Скан LiDAR (Pro) | Сделан честно | ✅ построено | `RoomCaptureRepresentableV2`, `RoomScanProgress`, `RoomGeometryExtractor` |
| **Путь без LiDAR** | Кнопка-заглушка, только Pro | ❌ нет + нет рантайм-гейта | `RoomScanFlowFeature:82,193`, `RoomScanFlowView:167` |
| **Движок расстановки** | Зрелый детерминированный | ⚠️ есть, но не на AR-пути | `GenerateArrangementTool:350+`, `AgentOrchestrator:159-206`, `PromptBuilder:85` |
| Latency / стриминг | «3-4 сек» приукрашены | ⚠️ LLM считает координаты, стрима нет | `yandexgpt.js:84` (`stream:false`), `ai-advisor:154` (timeout 25с) |
| **Каталог USDZ** | Загрузчик есть, каталога нет | ❌ 0 моделей → placeholder | `USDZLoader:30`, `FurnitureEntityFactory:80`, `DesignResponseParser:150` |
| **Шаг 4 «замена»** | Не существует | ❌ только tap/move/remove/refine | `ARDesignerFeature:73-93` |
| **Монетизация** | Только в документах | ❌ нет (мёртвый флаг + DENY) | `PermissionEngine:129` (`hasSubscription`), `MarketplaceFeature:100` (голый `openURL`) |
| Источник товаров | Партнёрский каталог | ❌ Apify-скрейпинг | `marketplace/index.js:34-35,143`, `apify-client.js` |
| Векторный поиск YDB | Нативный ANN/KNN | ⚠️ противоречит: full-scan + косинус | `rag-search.js:10`, `ydb-client.js` (только Scan/Get/Put) |
| Гибрид AI Lite+Pro | Рекомендация (~2,2₽) | ❌ зашит только Pro `yandexgpt-5/latest` | `yandexgpt.js:82`, `YandexGPTProvider:23` |
| Сохранение чата | Фаза 10 | ✅ построено (но в `Caches`) | `AIAdvisorFeature:192,267`, `StorageClient:60-92` |
| Сохранение проектов | Фаза 10 | ❌ скан → temp-USDZ → удаляется | `RoomScanFlowView:213-218`, `HomeFeature:104-136` |
| Петля возврата | Главный пробел | ❌ нет пушей/шеринга/портфолио | grep push/share/portfolio = 0 |

---

## 3. Принципы апгрейда

1. **Сохраняем логику.** Ядро (`AgentLoop`, `ToolRegistry`, `SkillIndex`, `Providers/*`, `AIProviderRouter`, `CircuitBreaker`, `yandexgpt.js`, `gigachat.js`, `triplex-fallback.js`) — только bugfix/добавления. Всё новое строим в фичах/новых модулях.
2. **Сначала функции и деньги, дизайн потом.** Визуальный редизайн (Apple-стиль из `demo-design-mg`) — **вне этого апгрейда** (см. §7). Новый стиль пока живёт для веб-лендинга.
3. **Бэкенд no-deps** на горячем пути AI (только `fetch`). npm-библиотеки (если нужны для GLB→USDZ) — только в **изолированном** сервисе конвертации.
4. **Переиспользуем, не дублируем.** AR-движок, `CollisionDetector`, `DesignNorms`, `RoomGeometry.init`, `runCollisionCheck` уже есть — подключаем их, а не пишем заново.
5. **Регуляторика — на входе, не на выходе.** Apple anti-steering (3.1.1), 152-ФЗ, ЗоЗПП учитываем в дизайне каждой денежной фичи (см. §6).

---

## 4. План по фазам

Фазы — из `BUSINESS_MODEL.md §11`, внутрь вложены приоритеты `UX_GROWTH.md §7`. Каждая фаза самодостаточна и даёт измеримый результат.

### Фаза 0 — Подготовка (сейчас, дни)

| # | Задача | Трудозатраты | 🔒 |
|---|--------|------|----|
| 0.1 | ✅ Бэкап (тег + ветка-архив) — **сделано** | — | |
| 0.2 | ✅ Перенести `BUSINESS_MODEL.md` / `UX_GROWTH.md` в основную ветку — **сделано** | — | |
| 0.3 | ✅ **Сделано** — выровнены `README.md` / `STRATEGY.md` под реальность (статус-пометки по аудиту + ссылки на `UPGRADE_PLAN.md`) | S | |
| 0.4 | Юр-база: уведомление РКН (обычные ПДн, не биометрия), оферты (пользователь+фабрика), нужные ОКВЭД | S (юристы) | |
| 0.5 | (опц.) Выровнять `maxTokens` между слоями (backend 2000 / iOS 1024 / Agent 2048) | S | 🔒 |

**Выход:** доки и юр-база соответствуют коду; команда стартует с верной картины.

---

### Фаза 1 — Массовый рынок + подписка (фундамент дохода №1)

**Зачем:** без массовых устройств нет пользователей, без пользователей нет подписчиков. Сначала открываем рынок и делаем генерацию быстрой/честной, затем включаем первый поток дохода.

| # | Задача | Зависит | Трудозатраты | 🔒 |
|---|--------|---------|------|----|
| **A1. Путь без LiDAR** [разрыв №1] | | | | |
| A1.1 | ✅ **Сделано** — экран `ManualRoomEntryView` (ширина/глубина/высота, валидация диапазонов + ≥4 м², предпросмотр площади) | — | M | |
| A1.2 | ✅ **Сделано** — фаза `RoomScanFlowPhase.manualEntry` + действия `.manualDimensionsSubmitted`/`.backFromManualEntryTapped`; построитель `RoomGeometry.manualRectangular(...)`; переиспользует `.geometryExtracted` → `styleSelection` | A1.1 | S | |
| A1.3 | ✅ **Сделано** — рантайм-детекция `RoomCaptureSession.isSupported` + адаптивный intro: без LiDAR primary-кнопка ведёт в ручной ввод (⚠️ полная проверка — только на реальном non-LiDAR устройстве; симулятор форсит `true`) | — | S | |
| A1.4 | ✅ **Сделано** — `ManualRoomEntryTests` (7 тестов: построитель + поток редьюсера), все зелёные; ⏳ ручной прогон на реальном устройстве без LiDAR | A1.1–3 | S | |
| **A2. Движок на AR-путь** [честная скорость] — ⏳ **отложено до сессии с устройством** (меняет поведение генерации, корректность AR-расстановки нужно проверять визуально на реальном скане) | | | | |
| A2.1 | Переключить `AgentOrchestrator.generateWithRetry`: LLM → **только список мебели + стиль**; координаты считает `GenerateArrangementTool`/`CollisionDetector`. **Аудит-находки (2026-06-09):** вход движка — `RoomAnalysis(roomDimensions: widthM/depthM/floorAreaM2)` + `[FurnitureSelectionItem]`; выход `ArrangementPlan.placements` → маппить в `[FurnitureItem]`. ⚠️ **Координаты: движок отдаёт УГОЛ предмета (`tryPlaceAt` клампит `x..x+w`), а `FurnitureItem`/`CollisionService` — ЦЕНТР → нужна конвертация `position = угол + размер/2` по x/z.** Начало координат (угол комнаты) и единицы (м) совпадают | — | L | |
| A2.2 | Переписать `PromptBuilder.systemPrompt`: убрать требование `position/rotation`, перенести retry с перепромпта LLM на детерминированную переукладку; `DesignResponseParser` — `position` опционально | A2.1 | S | |
| A2.3 | Latency-бенч (мок-LLM): зафиксировать реальное время до/после, чтобы заявления о скорости были подтверждены | A2.1 | M | |
| A2.4 | **Отложено** (решение владельца): стриминг текста-объяснения (SSE/chunked + `AsyncStream`). Сцену ускоряет движок (A2.1) и без него; затрагивает ядро → отдельным этапом после Фаз 1–3 | — | L | 🔒 |
| **A3. Подписка PRO** [доход №1] | | | | |
| A3.1 | Backend ЮKassa: новая Cloud Function `functions/payments` + `shared/yookassa.js`, приём платёжного вебхука (проверка подписи, идемпотентность), статус подписки в YDB по `userId` | — | L | |
| A3.2 | iOS: модель тарифа `enum FREE/PRO/BUSINESS`, хранение/синхронизация статуса с backend, отображение в UI | A3.1 | M | |
| A3.3 | iOS: пейволл-экран (TCA) + гейтинг фич — **оживить** `SessionContext.hasSubscription`, лимиты FREE (напр. N сканов/AI-запросов), показ пейволла при превышении | A3.2 | M | |

**⚠️ Apple (Guideline 3.1.1, anti-steering):** подписку продавать **на сайте** через ЮKassa; в приложении показывать ценность, не вести «в лоб» на внешнюю оплату цифровой подписки.

**Выход:** приложение работает на массовых iPhone (без LiDAR), генерация быстрая и стабильная, первые PRO-платящие → безубыточность с ~5–10 платящих.

---

### Фаза 2 — Каталог + партнёры (реальные модели вместо placeholder)

**Зачем:** сейчас в AR показывается цветной бокс, а товары — скрейпинг WB/Ozon. Чтобы «вау» работало и модель была легальной — нужен партнёрский каталог реальных USDZ.

| # | Задача | Зависит | Трудозатраты | 🔒 |
|---|--------|---------|------|----|
| B1 | B2B-конвейер GLB→USDZ: приём GLB от фабрик, batch-конвертация (Mac mini + `usdzconvert`), проверка полигонов, заливка в Object Storage, запись каталога (article, категория, габариты, стиль, `usdzURL`, цена). Бэкенд + offline-тулинг. **Для первых партнёров оцифровка бесплатна** (наполнить каталог, снять cold start) | — | XL | |
| B2 | Партнёрский каталог в YDB (таблица `products`) + `shared/partner-catalog.js`; переключить `marketplace/index.js` с Apify на каталог **за фичефлагом** «партнёры vs Apify» (фолбэк на cold start) | B1 | L | |
| B3 | Резолвинг `article/SKU → USDZ-URL` (backend-эндпоинт + iOS-клиент): по артикулу/атрибутам ищет запись каталога, отдаёт `usdzURL` + цену + ссылку | B2 | L | |
| B4 | Подключить резолвер в пайплайн дизайна: заполнять `FurnitureItem.usdzURL`/цену из каталога по article **вместо** доверия `rawItem.usdz_url`; `PromptBuilder` — LLM возвращает артикул/атрибуты, не URL модели | B3 | M | |
| B5 | Векторный поиск товаров на стороне YDB: **PoC** — реально ли KNN/YQL через доступный из Cloud Function интерфейс; иначе честный SQL-пре-фильтр (категория + габариты ±15% + стиль). Расширить `ydb-client.js` методом `query/executeYql` (добавление) | — | L | |
| B6 | Починить RAG-поиск дизайн-знаний: убрать full-scan `scan(limit 500)` + косинус-в-Node (нарушает §9.2), вынести ранжирование на сторону YDB / пре-фильтр | — | M | |
| **B7. Гибрид AI Lite+Pro** [рычаг затрат — решено: Фаза 2] | | | | |
| B7.1 | Роутер выбора модели Lite vs Pro по типу/сложности запроса — **в `ai-advisor/index.js`** (перед `triplexFallback`, чтобы не трогать ядро) | — | L | |
| B7.2 | Поддержка `yandexgpt-lite` в провайдере (выбор `modelUri`) | B7.1 | M | 🔒 |
| B7.3 | Инструментирование `usage` (prompt/completion tokens) + фактическая модель в логи — измерить реальную цену запроса | B7.1 | S | 🔒 |

**Выход:** каталог 200+ моделей, AI подбирает **реальную** мебель по артикулу, scraping выключен; (опц.) средняя цена AI-запроса ~2,2₽ вместо ~13₽.

---

### Фаза 3 — CPA + «купить» (главный двигатель прибыли)

**Зачем:** поток дохода №2 (CPA 12%) — основная прибыль. Плюс самая залипательная фича (замена предмета) как PRO-триггер.

| # | Задача | Зависит | Трудозатраты | 🔒 |
|---|--------|---------|------|----|
| **C1. Шаг 4 «заменить на похожий»** [приоритет №4, PRO-триггер] | | | | |
| C1.1 | Новый поток в `ARDesignerFeature`: `replaceItemRequested(id)` → `alternativesLoaded([...])` → `itemReplaced(old:new:)`; состояние `alternatives/isLoading/replaceTargetID`; обновлять `state.prices` | Фаза 2 (каталог) | M | |
| C1.2 | UI-карточка «4 похожих» в `ARDesignerComponents` (миниатюра/цена/габариты + «Заменить») | C1.1 | M | |
| C1.3 | Атрибутный поиск «похожих ±10–15% по габаритам + категория + стиль» поверх `SearchMarketplaceFurnitureTool` (сейчас на mock — нужен реальный API) | Фаза 2 | M | |
| C1.4 | **Явный** пере-чек коллизий после замены (вызвать существующий `runCollisionCheck`; внимание: `refineCompleted` коллизии НЕ перепроверяет) | C1.1 | S | |
| C1.5 | Пейволл-гейт на замену в FREE | A3.3 | S | |
| **C2. CPA-атрибуция** [узкое место] | | | | |
| C2.1 | Динамические одноразовые промокоды (генерация/валидация, TTL 60–90 дн., привязка к userId в YDB) | A3.1 | L | |
| C2.2 | Кнопка «Купить» с deeplink + партнёрской меткой/UTM (сейчас — голый `openURL` без атрибуции) | B3 | M | |
| C2.3 | Вебхук приёма постбэков от фабрик + сверка/атрибуция продаж; интеграция с CRM (1С-Битрикс/RetailCRM) | C2.1 | XL | |
| C3 | «Купить весь интерьер» (корзина из всех предметов) + **BNPL** на карточке (Подели/Сплит/Долями) | B3 | M–L | |
| C4 | Дашборд фабрики (примерки, клики, лиды) | C2.3 | M | |

**Способ CPA-атрибуции** (промокоды B+ / оплата-внутри A) — решается на **старте Фазы 3** (решение владельца отложено). В плане держим оба варианта.

**⚠️ Регуляторика:** deeplink на стороннюю оплату — снова риск Apple 3.1.1; передача userId в вебхук — 152-ФЗ (согласие + политика). Целевая модель — оплата внутри + сплит ЮKassa (атрибуция 100%, комиссия 12%); промокоды — мостик (эффективно 9%).

**Выход:** первые продажи с комиссией и автосверкой.

---

### Фаза 4 — Рост + удержание (LTV)

**Зачем:** «после одной комнаты нет повода вернуться» — главный пробел удержания. Здесь строим петлю возврата и масштаб.

| # | Задача | Зависит | Трудозатраты | 🔒 |
|---|--------|---------|------|----|
| D1 | **ProjectStore**: персистентность реальных проектов комнат (`RoomProject`: id/имя/дата/превью/USDZ/стиль/бюджет/расстановка), сохранение при завершении дизайна, чтение на Home (сейчас скан → temp-USDZ → удаляется) | — | L | |
| D2 | Экран «Мои комнаты/проекты»: обработать `allProjectsTapped` (сейчас → `.none`), список с превью и входом обратно в проект | D1 | M | |
| D3 | Перенести хранилище с `Caches` (ОС чистит) на Application Support/Documents + персист `AgentSession` (Codable-снапшот, без правок `AgentLoop`) | — | M | |
| D4 | Re-engagement: локальные уведомления (`UNUserNotificationCenter`) + `BGTaskScheduler`, сезонные подсказки, «новый товар подходит к вашей <комнате>» + entitlement | D1 | L | |
| D5 | Шеринг «было/стало» (`ShareLink`, фото ↔ AR-результат) + базовое портфолио; в FREE — водяной знак (пейволл-триггер) | D1 | L | |
| D6 | Масштабирование конвертации GLB→USDZ (macOS-облако/ферма Mac mini) при росте партнёров | B1 | M |  |

**Выход:** ~500–1000 пользователей, прибыль ~80–100к₽/мес, работающая петля возврата.

---

## 5. Защищённое ядро — правки, требующие согласования владельца

Эти задачи затрагивают файлы из списка «не менять без запроса» (`CLAUDE.md`). Все — **добавления/bugfix**, не редизайн, но требуют явного «ок»:

| Задача | Файлы | Характер |
|--------|-------|----------|
| A2.4 Стриминг текста — **отложено** | `yandexgpt.js` (`stream`), `AIProviderRouter`, `AIProviderProtocol`, переосмыслить Circuit Breaker/таймауты под стрим | добавление метода |
| B7.2 Поддержка lite-модели | `yandexgpt.js:82` (`modelUri`), `YandexGPTProvider.swift:23`; сигнатура `callYandexGPT` → `triplex-fallback.js` | добавление |
| B7.3 Проброс `usage` | `triplex-fallback.js` (usage теряется) | добавление поля |
| 0.5 Выравнивание maxTokens | `AgentLoop.swift:335` (2048→2000) | bugfix |

**Рекомендация:** роутер модели (B7.1) разместить в `ai-advisor/index.js` (НЕ ядро) — тогда из ядра трогаем только B7.2/B7.3 (минимально).

---

## 6. Риски и зависимости

1. **CPA-атрибуция (C2)** — главный риск бизнес-модели: длинный цикл покупки мебели, слабые CRM фабрик «теряют» продажи, договоры/сверка. Без неё поток №2 (главная прибыль) не работает.
2. **Cold start у партнёров** — без каталога нет CPA и нет реальных USDZ. Нужен нетворкинг и бесплатная/льготная оцифровка первым фабрикам. Фичефлаг «партнёры vs Apify» (B2) — страховка.
3. **GLB→USDZ привязан к macOS** — не запускается в Cloud Function. Решается batch-конвертацией на Mac mini (`usdzconvert`); оплата зарубежного macOS-хостинга из РФ затруднена.
4. **Векторный поиск YDB (B5)** — KNN/YQL может быть недоступен через текущий Document API (DynamoDB-совместимый) → сначала PoC, иначе SQL-пре-фильтр. Не нарушить no-deps.
5. **Apple anti-steering (3.1.1)** — пронизывает A3, C1.5, C2: оплату и deeplink проектировать аккуратно, иначе отклонение ревью.
6. **Реальный marketplace API — пока mock** (`SearchMarketplaceFurnitureTool.performRealSearch` = TODO): для шага 4 (C1) и BNPL нужен рабочий backend-поиск.
7. **Рассинхрон систем координат** (A2): LLM-плоскость (угол 0,0,0) vs simd-центр в `CollisionService` — нужны тесты при переключении на движок.

---

## 7. Что НЕ входит в этот апгрейд

- **Визуальный редизайн приложения** (Apple-стиль из `demo-design-mg`) — отложен по решению владельца («сначала функции и деньги»). Новый стиль остаётся для веб-лендинга; план не привязан к нему. Вернёмся к редизайну UI как к отдельному этапу после Фаз 1–3.
- **Облачная синхронизация сессий (YDB, B2-уровень)** и **Auth (Apple/Яндекс/VK ID)** — в roadmap, но за рамками первых фаз монетизации.

---

## 8. Решения владельца

Зафиксировано 2026-06-09:

| Вопрос | Решение | Влияние на план |
|--------|---------|-----------------|
| Стриминг (A2.4) | **Отложить** | Убран из Фазы 1; сцену ускоряет движок (A2.1) и без него. Стрим — отдельным этапом после Фаз 1–3 (затрагивает ядро) |
| Гибрид Lite+Pro (B7) | **Фаза 2** | Делаем после критичных функций Фазы 1; роутер модели — вне ядра (в `ai-advisor`) |
| CPA-атрибуция (C2) | **Решить на старте Фазы 3** | В плане держим оба варианта (промокоды B+ / оплата-внутри A); выбор — перед Фазой 3 |
| Оцифровка GLB→USDZ для первых фабрик | **Бесплатно для первых** | B1/B2: первым партнёрам — бесплатная оцифровка ради наполнения каталога (снять cold start) |
| Движок на AR-путь (A2) | **Отдельным заходом с устройством** | Меняет поведение генерации (координаты движка вместо LLM); корректность AR-расстановки проверяется визуально на реальном скане. Свап угол→центр и контракты движка разведаны — см. A2.1 |

**Осталось решить (не блокирует старт):**
- **BNPL-провайдер (C3):** Подели / Яндекс Сплит / Долями — выбрать ближе к Фазе 3 (по охвату обычно начинают с Яндекс Сплит).

---

> Этот план — каркас на основе аудита кода и двух стратегических доков. Статусы и оценки обновляйте по мере реализации. Источник истины по нормам — `DesignNorms.swift`; по ценам — `STRATEGY.md §1.3` / `BUSINESS_MODEL.md §4`.
